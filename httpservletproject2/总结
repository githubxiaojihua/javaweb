注册实例
http://localhost:8080/demo2/register.htm
使用了一个beanutil拷贝bean属性

create table customer(
		id int primary key auto_increment,
		username varchar(20),
		password varchar(20),

		email varchar(20),
		name varchar(20),
		sex varchar(10),

		birthday date
	);



使用jsp完善了登录案例
http://localhost:8080/demo2/login.jsp